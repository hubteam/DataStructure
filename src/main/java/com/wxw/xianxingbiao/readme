线性表的存储结构：
1、顺序表：顺序存储结构，连续的存储空间
      特点：随机访问性，
                 存储空间静态分配
                 线性表插入要移动多个元素
                 存储密度=1
2、链表：链式存储结构
       特点：不支持随机访问，
                  节点的存储空间利用率比顺序表稍微低一些，支持存储空间动态分配，需要在新的节点时候再行空间划分。动态分配
                  链表中进行插入无需移动元素，只需要修改指针
                  存储密度《1(因为节点中有指针域)