栈：
1、在一端（栈顶）进行插入、删除的线性表，栈底是固定不变的，先进后出
（1）两个状态：空：top==-1,满：top==maxSize
（2）两个操作：进栈：先移动指针，再进入元素
                              出栈：先取出元素，在变动指针
（3）栈满入栈是上溢
          栈空出栈是下溢
                              
                              
队列：
1、队尾插入，队头删除，先进先出
2、循环队列：rear指向队尾，指向刚进队的元素，front队头，指向刚出队的元素
         元素进队的时候，rear向后移动，元素出队的时候，front向后移动
         两个元素最终会到达数组末端 max-1的地方，此时队列空，但是不能进队，这就是假溢出，改进用循环队列：数组成环
         队列空：rear==front
         队列满：(rear+1)%max == front